<!DOCTYPE html>
<html lang="en" class="h-100 w-100">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css" type="text/css">

    <title>Band Together</title>
</head>
<body class="h-100 w-100">
    <nav class="navbar navbar-light bg-light shadow-sm">
            <section class="nav nav-justified">
                <header>
                    <a class="navbar-brand" href="/">Band Together</a>
                </header>
            </section>
            <section class="nav mr-auto">
                <a class="nav-link" href="/bands">Browse</a>
                {{#if loggedInUsername}}
                <a class="nav-link" href="/bands/browseUserBands">My band</a>
                {{/if}}
            </section>
            <section class="nav justify-content-right">
                {{#if loggedInUsername}}
                <a class="nav-link" href="/account/view/{{loggedInUsername}}">{{loggedInUsername}}</a>
                {{/if}}
                <a class="nav-link btn btn-primary btn-sm" href="{{#if loggedInUsername}}/account/logout{{else}}/signinup{{/if}}">{{#if loggedInUsername}}Sign out{{else}}Sign in/up{{/if}}</a>
            </section>
    </nav>
    <main class="p-3 container-fluid">
        {{{body}}}
    </main>
    <footer class="bg-light border-top m-auto p-2 h-auto">
        <p>Hello everybody</p>
    </footer>
</body>
</html>